{
  "name": "etiquetas-desktop",
  "version": "2.0.73",
  "description": "Aplicativo para impressão de etiquetas 40x60mm com QR Code em impressoras Argox OS-2140",
  "main": "src/main/index.js",
  "scripts": {
    "start": "electron .",
    "dev": "electron . --dev",
    "test": "node --test",
    "build:test": "node scripts/generate-runtime-config.js test && electron-builder --win --config electron-builder.test.json --publish never",
    "build:beta": "node scripts/generate-runtime-config.js beta && electron-builder --win --config electron-builder.beta.json --publish never",
    "build:prod": "node scripts/generate-runtime-config.js prod && electron-builder --win --config electron-builder.prod.json --publish never",
    "publish:test": "node scripts/generate-runtime-config.js test && electron-builder --win --config electron-builder.test.json --publish always",
    "publish:beta": "node scripts/generate-runtime-config.js beta && electron-builder --win --config electron-builder.beta.json --publish always",
    "publish:prod": "node scripts/generate-runtime-config.js prod && electron-builder --win --config electron-builder.prod.json --publish always",
    "rebuild": "electron-rebuild"
  },
  "author": "LOOPII",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/dfcomsoftwarescompany/etiquetas-desktop.git"
  },
  "devDependencies": {
    "electron": "^28.0.0",
    "electron-builder": "^24.9.1",
    "electron-rebuild": "^3.2.9"
  },
  "dependencies": {
    "canvas": "^3.2.0",
    "cors": "^2.8.5",
    "electron-log": "^5.4.3",
    "electron-updater": "^6.1.7",
    "express": "^4.18.2",
    "qrcode": "^1.5.3",
    "socket.io-client": "^4.8.3"
  }
}
