{
  "name": "etiquetas-desktop",
  "version": "2.0.10",
  "description": "Aplicativo para impressão de etiquetas 40x60mm com QR Code em impressoras Argox OS-2140",
  "main": "src/main/index.js",
  "scripts": {
    "start": "electron .",
    "dev": "electron . --dev",
    "build": "electron-builder",
    "build:win": "electron-builder --win",
    "build:test": "electron-builder --win --publish never",
    "publish:test": "electron-builder --win --publish always --config.publish.releaseType=draft",
    "publish:prod": "electron-builder --win --publish always",
    "rebuild": "electron-rebuild"
  },
  "author": "DFCOM",
  "license": "MIT",
  "devDependencies": {
    "electron": "^28.0.0",
    "electron-builder": "^24.9.1",
    "electron-rebuild": "^3.2.9"
  },
  "dependencies": {
    "canvas": "^3.2.0",
    "cors": "^2.8.5",
    "electron-log": "^5.4.3",
    "electron-updater": "^6.1.7",
    "express": "^4.18.2",
    "qrcode": "^1.5.3"
  },
  "build": {
    "appId": "com.dfcom.etiquetas",
    "productName": "Etiquetas DFCOM",
    "directories": {
      "output": "dist"
    },
    "win": {
      "target": [
        "nsis"
      ],
      "icon": "assets/icon.ico"
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true,
      "createDesktopShortcut": true,
      "createStartMenuShortcut": true
    },
    "publish": {
      "provider": "github",
      "owner": "dfcomsoftwarescompany",
      "repo": "etiquetas-desktop",
      "releaseType": "release",
      "private": true
    },
    "extraResources": [
      "assets/**"
    ]
  }
}
